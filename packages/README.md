# Future shared packages

No runtime package has been moved into this directory yet.

- The existing public Next.js frontend remains at the repository root temporarily.
- `packages/database` is reserved for the shared Prisma/PostgreSQL package in a future Phase 2 checkpoint.
- Shared contracts, validation, configuration, logging, and security packages will be added only when they have real consumers.
- No microservice exists yet.
